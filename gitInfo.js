/*
    For this section of the assessment you will be putting together a cheat sheet for common git commands.  
    You'll provide the command as well as what it does. 
*/

//////////////////PROBLEM 1////////////////////
/*
    Create a variable called 'gitDefinition'.
    It should be a string containing your best definition of what Git is.
*/

//CODE HERE

let gitDefinition = `is a program. Its a type of program called a version
 control system. Using code language you can make commands to perform actions with code.
 The best uses of the library of commands are are for softward engineers, and tech teams
  to track projects. You can see overviews of projects,
  make 'checkpoints' to debug, and 'save'`
//////////////////PROBLEM 2////////////////////
/*
    Create a variable called 'gitHubDefinition'.  
    It should be a string containing your best definition of what GitHub is.
*/
//CODE HERE
let gitHubDefinition = `A website with features centered around coding programs.
Much like a social media profile, users can upload code, discuss in forum problems with code,
connect with other coders, network, and share work.`

//////////////////PROBLEM 3////////////////////
/*
    Create a variable called 'gitInitDefinition'.  
    It should be a string containing your best definition of what 'git init' does.
*/

//CODE HERE

let gitInitDefinition = `While using the command line interface
in a certain folder, this command initializes a repository --the
most basic unit of git `
//////////////////PROBLEM 4////////////////////
/*
    Create a variable called 'gitCloneDefinition'.  
    It should be a string containing your best definition of what 'git clone' does.
*/

//CODE HERE

let gitCloneDefinition = `this 'copy'style command will point to 
a repository and make a replica of it in a new location`
//////////////////PROBLEM 5////////////////////
/*
    Create a variable called 'gitStatusDefinition'.  
    It should be a string containing your best definition of what 'git status' does.
*/

//CODE HERE

let gitStatusDefinition = `while working in a folder in your 
command line, this command 'prints' or 'shows' the status of 
current work station. specifically which files in your folder are 
being tracked by git, (if files show missing they aren't being tracked), 
and what has been staged or not.`
//////////////////PROBLEM 6////////////////////
/*
    Create a variable called 'gitAddDefinition'.  
    It should be a string containing your best definition of what 'git add' does.

    Create another variable called 'gitAddCode'.  
    It should be a string containing the code to add all files.
*/

//CODE HERE

let gitAddDefinition = `this command adds some files to be tracked
by git`
//////////////////PROBLEM 7////////////////////
/*
    Create a variable called 'gitCommitDefinition'.  
    It should be a string containing your best definition of what 'git commit' does.

    Create a variable called 'gitCommitCode'.  
    It should be a string containing the code to commit using the message "initial commit".
*/

//CODE HERE
let gitCommitDefinition = `creates a snapshot of the files your are tracking`
let gitCommitCode = `git commit -m "initial commit"`

//////////////////PROBLEM 8////////////////////
/*
    Create a variable called 'gitPushDefinition'.  
    It should be a string containing your best definition of what 'git push' does.
*/

//CODE HERE

let gitPushDefinition = `all files being tracked that have a commit will be sent
to a specified git repository storage location`